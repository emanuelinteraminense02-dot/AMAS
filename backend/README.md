# Back-end AMAS

Projeto Spring Boot responsavel pela API e regras de negocio.

## Execucao local

```powershell
cd backend
.\gradlew.bat bootRun
```

A API sobe por padrao em `http://localhost:8080/api`.

Use `Java 17` ou superior.
